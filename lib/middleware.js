// imports

module.exports = {
    // werite middleware/headers
    // write middleware/validate-session
    // write middleware/validate-admin

    // run middleware set up

    // write middleware auth set up
}